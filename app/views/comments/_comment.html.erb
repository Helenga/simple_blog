<h4><%= comment.commenter %> said <%= distance_of_time_in_words(comment.created_at, Time.now) %> ago:</h4><br>
<%= comment.body %>
<%= link_to 'Destroy comment', [comment.article, comment],
						method: :delete, 
						data: { confirm: 'Are you sure?'} %>
